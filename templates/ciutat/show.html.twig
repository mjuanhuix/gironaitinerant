{% extends 'base.html.twig' %}

{% block title %}Ciutat{% endblock %}

{% block body %}
    <h1>Ciutat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ciutat.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ciutat.nom }}</td>
            </tr>
            <tr>
                <th>Cp</th>
                <td>{{ ciutat.cp }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ciutat_index') }}">Tornar a la llista</a>

    <a href="{{ path('ciutat_edit', {'id': ciutat.id}) }}">edit</a>

    {{ include('ciutat/_delete_form.html.twig') }}
{% endblock %}
